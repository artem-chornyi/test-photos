const styles = theme => ({
    list: {
        display: 'flex',
        flexWrap: 'wrap',
        flexDirection: 'row',
        justifyContent: 'center',
        gap: 20
    }
});

export default styles;