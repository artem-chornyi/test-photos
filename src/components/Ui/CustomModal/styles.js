import { colors } from "../../../theme/colors";

const styles = theme => ({
    imgBox: {
        position: 'absolute',
        top: '50%',
        left: '50%',
        transform: 'translate(-50%, -50%)',
        boxShadow: 24,
        maxWidth: 600,
        maxHeight: 600,
    }
})

export default styles;